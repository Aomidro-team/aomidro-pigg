import { createAction } from 'redux-act';

export const changeInitPos = createAction('change initPos');
export const changeUsers = createAction('change users');
export const updateCurrentPos = createAction('update currentPos');
export const setSelfGoalPos = createAction('set selfGoalPos');
export const changePos = createAction('change pos');
